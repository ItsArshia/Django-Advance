services:
  backend:
    build: .
    container_name: backend
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - ./core/:/app # bind mount
    ports:
      - "80:8000"  
    environment:
      - SECRET_KEY=test
      - DEBUG=True
